/*
Exercise No. 02

The SQL code that creates the tables for this exercise is in the file create.sql.

Create a query that displays the unique first names from the employee table.
*/
SELECT
    DISTINCT first_name
FROM
    Employee;