/*
Exercise No. 05

The SQL code that creates the tables for this exercise is in the file create.sql.

Create a query that extracts all the countries from the country table sorted in ascending order by country_name.
*/
SELECT
    *
FROM
    country
ORDER BY
    country_name ASC;