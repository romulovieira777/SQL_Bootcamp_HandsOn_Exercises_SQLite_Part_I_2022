/*
Exercise No. 09

The SQL code that creates the tables for this exercise is in the file create.sql.

Create a query that displays the first ten rows the job table.
*/
SELECT
    *
FROM
    job
LIMIT
    10;